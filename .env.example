# Backend — required for database and auth
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Get a fresh connection string from https://console.neon.tech
# If you see "could not translate host name" — your Neon project may be suspended
# or the URL is outdated. Create a new project or copy the latest connection string.
